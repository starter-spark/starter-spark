// Same layout as /learn; duplicated into this segment because the (blocking)
// group hosts the [product] routes (see (blocking) note in shop/[slug]/page.tsx).
export { default } from '../../learn/layout'
